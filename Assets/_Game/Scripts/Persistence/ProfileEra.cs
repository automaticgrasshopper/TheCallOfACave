namespace TCC.Persistence
{
    /// <summary>
    /// Stable values stored in a player profile. Never reorder or reuse a value.
    /// </summary>
    public enum ProfileEra
    {
        First = 1,
        Second = 2,
        Third = 3,
        NewAge = 4
    }
}
