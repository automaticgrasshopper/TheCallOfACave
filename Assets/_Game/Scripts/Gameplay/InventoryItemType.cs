namespace TCC.Gameplay
{
    /// <summary>Stackable cargo types shown in the colony backpack.</summary>
    public enum InventoryItemType
    {
        Food,
        MetalScrap,
        RefinedComponent,
        EliteEquipment
    }
}
